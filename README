Android Remote
================

This project makes your Android phone as a remote for your computer

Installation
================

Run the RemoteBluetoothServer project in your system and install the RemoteBluetooth app in your Android phone.

Running Remote Server
---------------------

If you have to run the server in linux, then make sure you install the libbluetooth-dev package.

If you have to run the server in mac, configure eclipse to pass the -d32 JVM argument.
